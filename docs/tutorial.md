# Tutorial — From Zero to Prototype

This document explains how to set up the environment, create POI data,
build image embeddings, and run the offline demo.

## Installation

```bash
python -m venv .venv
source .venv/bin/activate  # or Windows equivalent
pip install -r requirements.txt

